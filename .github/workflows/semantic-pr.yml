name: semantic-pr

# pull_request_target is required for this action to read PR titles from fork PRs.
# IMPORTANT: Never add an actions/checkout step to this workflow. Checking out fork
# code under pull_request_target grants the fork's code access to repository secrets.
on:
  pull_request_target:
    types: [opened, edited, synchronize]
    branches: [main]

permissions:
  pull-requests: read

jobs:
  semantic-pr:
    name: Conventional commit PR title
    runs-on: ubuntu-latest
    steps:
      - uses: amannn/action-semantic-pull-request@v6.1.1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          types: |
            feat
            fix
            docs
            style
            refactor
            perf
            test
            build
            ci
            chore
            revert
          requireScope: false
          subjectPattern: ^(?![A-Z]).+$
          subjectPatternError: >
            The subject "{subject}" found in the pull request title "{title}"
            must not start with an uppercase letter. Use lowercase to follow
            conventional commit conventions.
